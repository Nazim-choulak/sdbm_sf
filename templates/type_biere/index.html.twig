{% extends 'base.html.twig' %}

{% block title %}TypeBiere index{% endblock %}

{% block body %}
    <h1>TypeBiere index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for type_biere in type_bieres %}
            <tr>
                <td>{{ type_biere.id }}</td>
                <td>{{ type_biere.nom }}</td>
                <td>
                    <a href="{{ path('app_type_biere_show', {'id': type_biere.id}) }}">show</a>
                    <a href="{{ path('app_type_biere_edit', {'id': type_biere.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_type_biere_new') }}">Create new</a>
{% endblock %}
