{% extends 'base.html.twig' %}

{% block title %}TypeBiere{% endblock %}

{% block body %}
    <h1>TypeBiere</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_biere.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ type_biere.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_type_biere_index') }}">back to list</a>

    <a href="{{ path('app_type_biere_edit', {'id': type_biere.id}) }}">edit</a>

    {{ include('type_biere/_delete_form.html.twig') }}
{% endblock %}
